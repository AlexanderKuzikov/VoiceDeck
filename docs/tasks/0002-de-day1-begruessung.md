# Задача 0002 — DE Day 1: пак «приветствия и вежливость»

> Проект: VoiceDeck. Статус: открыта. Дата: 2026-09-16.

## Задание

Сгенерируй юнит `library/de/2026-09-17-begruessung/`: 5 фраз A1 (приветствия,
вежливость, первое знакомство) с русскими переводами. Без story (первые две
недели немецкого — только словарный запас). Голоса: лицо de-f, оборот ru-f.

Входит:
- phrases.txt (фраза<TAB>перевод), cards.tsv (= phrases), meta.json (kind: pack)
- audio/*.mp3: каждая фраза normal + slow
- anki.tsv

Не входит: story, диалоги, английский трек, правки CLI.

Старт: шаблоны в library/_template; команды CLI в AGENTS.md проекта.

Приёмка:
- `ls library/de/2026-09-17-begruessung library/de/2026-09-17-begruessung/audio`
- каждый mp3 > 5 КБ; строк в anki.tsv = 5
- `python -c "import json;json.load(open('library/de/2026-09-17-begruessung/meta.json'))"`

Ограничения: только после приёмки 0001; коммит+пуш своего номера; чужие файлы не трогать.

## Порядок работы

Прочитай AGENTS.md и контекст → сделай → проверь по приёмке → допиши отчёт ниже → статус «на проверке», жди.

## Отчёт сессии

Пусто.
